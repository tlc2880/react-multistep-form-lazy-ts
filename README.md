# React + Lazy Loading + TypeScript + Vite

# React Multi-Step Registration Form
# Using Form wrapper, custom hook, reg-ex validation, and no libraries
### To install: npm install
### To run: npm run dev